Logged in as <%=  current_user.username %>
<h1> Feed </h1>

<div class="container">
    <div class="container-pins">
    	<% @tweets.each do |tweet| %>
	    	<%= tweet.id %>
	    	<%= tweet.msg %></br></br>
    	<% end %>
    </div>
</div>